spring:
  application:
    name: invoice-core-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    openfeign:
      micrometer:
        enabled: true
      client:
        config:
          default:
            connectTimeout: 3000
            readTimeout: 10000
            loggerLevel: basic
          invoice-ocr-service:
            connectTimeout: 3000
            readTimeout: 35000
          invoice-file-service:
            connectTimeout: 3000
            readTimeout: 10000
      circuitbreaker:
        enabled: true
        alphanumeric-ids:
          enabled: true

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        failureRateThreshold: 50
        minimumNumberOfCalls: 10
        slidingWindowSize: 20
        waitDurationInOpenState: 30s
        permittedNumberOfCallsInHalfOpenState: 3
        slowCallDurationThreshold: 5s
        slowCallRateThreshold: 80
  timelimiter:
    configs:
      default:
        timeoutDuration: 35s
        cancelRunningFuture: true

management:
  observations:
    key-values:
      application: ${spring.application.name}
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  otlp:
    metrics:
      export:
        enabled: false
  tracing:
    propagation:
      produce:
        - W3C
      consume:
        - W3C
    sampling:
      probability: 1.0

invoice:
  core:
    file-service:
      base-url: ${INVOICE_FILE_SERVICE_BASE_URL:http://localhost:8081}
    ocr-service:
      base-url: ${INVOICE_OCR_SERVICE_BASE_URL:http://localhost:8083}
    common-service:
      base-url: ${INVOICE_COMMON_SERVICE_BASE_URL:http://localhost:8088}
    recognition:
      timeout-seconds: ${INVOICE_RECOGNITION_TIMEOUT_SECONDS:30}
      next-poll-after-ms: ${INVOICE_RECOGNITION_NEXT_POLL_AFTER_MS:1000}
      source: ${INVOICE_RECOGNITION_SOURCE:CAMERA}
